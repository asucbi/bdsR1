# use this _site.yml for consistent design of all labs; outputs knitted html to docs for immediate publication
name: "BDSR1"
output_dir: "../docs/hws"
output:
  tufte::tufte_html:
    css: assets/hw.css
    tufte_variant: "envisioned"
    highlight: pygments
  # tufte::tufte_handout:
  #   highlight: pygments
